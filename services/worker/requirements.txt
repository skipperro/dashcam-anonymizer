# Core Python requirements
python-dotenv>=1.0.0

# Video Processing
ffmpeg-python>=0.2.0
opencv-python>=4.8.0

# AI/ML for Object Detection
ultralytics>=8.0.0
# NOTE: torch and torchvision are installed with CUDA support in the Dockerfile before this file.
# Listing them here keeps them as declared dependencies; pip will skip reinstall if already satisfied.
torch>=2.0.0
torchvision>=0.15.0
lap>=0.5.12  # Required by ultralytics for object tracking (ByteTrack/BotSort)

# Array Operations and Data Processing
numpy>=1.24.0

# Message Queue Integration
pika>=1.3.0

# Object Storage Access
boto3>=1.26.0

# HTTP and API
requests>=2.31.0
fastapi>=0.100.0
uvicorn>=0.23.0

# Utilities
Pillow>=10.0.0
tqdm>=4.65.0

# Development and Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # Required for FastAPI TestClient
black>=23.0.0
flake8>=6.0.0

# Logging and Monitoring
structlog>=23.1.0

# Memory Profiling
pympler>=0.9
