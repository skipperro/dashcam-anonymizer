# RabbitMQ Configuration for Dashcam Worker
loopback_users.guest = false
listeners.tcp.default = 5672
management.listener.port = 15672

# Web-STOMP configuration for frontend WebSocket connections  
web_stomp.tcp.port = 15674

# Storage settings
disk_free_limit.absolute = 1GB

# Note: Definitions will be imported via init script after user creation
